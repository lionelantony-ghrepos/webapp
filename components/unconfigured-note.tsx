export function UnconfiguredNote() {
  return (
    <p className="font-serif italic leading-relaxed text-mist">
      The desk is not connected on this machine. Sign-in and requests need the
      InsForge URL and anon key in a local env file. The four stays still show
      from the catalog fallback.
    </p>
  );
}
